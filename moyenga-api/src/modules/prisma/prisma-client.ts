/**
 * Point d'entrée unique vers le client Prisma généré.
 * Le reste du code importe Role, Prisma, types, etc. depuis ce fichier
 * plutôt que de recalculer un chemin relatif vers prisma/generated/client.
 */
export * from '../../generated/prisma/client.js';
